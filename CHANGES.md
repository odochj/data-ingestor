# Changelog

## 2026-08-01

- Fixed JSON serialization in the data-ingestor API endpoints used by downstream consumers.
- Added API serialization tests for `/sources`, `/sources/{name}`, and `/facts`.
- Added basic test harness docs and dev requirements for local pytest runs.
